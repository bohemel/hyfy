#include <stdio.h>

#include "response.h"
#include "request.h"

void http_response_send(const char *resp_code, struct http_request *request, const char *body) {
	char outbuffer[MAX_OUTBUFFER_SIZE];
	return;
}
